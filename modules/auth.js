var request = require('request'),
	MapResponse = require('../response_body_mapper/MapResponse'),
	constants = require('../constants');

// const URL = constants.BASE_PATH + constants.API_PATH + "/login";

module.exports.login = function(params, callback) {
	callback(params);
};